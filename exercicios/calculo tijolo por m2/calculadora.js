/**
 * Calcular tijolos por metro quadrado
 * @author Lucas Santos da Silva
 */

let altura, comprimento, metro_quadrado, rs

function calcular(){
    altura = Number(frmFlex.txtnota1.value)
    comprimento = Number(frmFlex.txtnota2.value)
    altura = Number(frmFlex.txtnota1.value)
}
